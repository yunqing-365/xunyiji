# 忽略环境变量文件（绝不能把 API Key 传到 GitHub！）
.env

# 忽略 Node.js 下载的庞大依赖包（GitHub 不需要这个，别人下载后自己 npm install 即可）
node_modules/
package-lock.json

# 忽略操作系统的隐藏文件
.DS_Store
Thumbs.db

# --- Hardhat & Web3 忽略名单 ---
node_modules/
package-lock.json

# Hardhat 编译生成的缓存和庞大产物
cache/
artifacts/
ignition/deployments/

# 环境变量（绝对不能把私钥或 API Key 传上去）
.env

# 操作系统自动生成的垃圾文件
.DS_Store